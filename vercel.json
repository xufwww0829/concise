{
  "buildCommand": "hugo --gc --minify",
  "outputDirectory": "public",
  "installCommand": "hugo version || echo 'Hugo not required to install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
